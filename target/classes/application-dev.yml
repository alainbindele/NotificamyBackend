# Development profile configuration
spring:
  config:
    activate:
      on-profile: dev

logging:
  level:
    com.notifyme: DEBUG
    org.springframework.web: DEBUG
    org.springframework.security: DEBUG

server:
  port: 8080